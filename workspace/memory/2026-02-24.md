## New Agents Created and Registered
1. **Hans (Coder)**:
   - Purpose: Writes production-ready code with a focus on TDD and REST-first principles.
   - Registered in `openclaw.json` under ID `hans`.
   - Workspace created at `/home/claw/.openclaw/workspace/agents/hans`.

2. **Paddock (Email / Social Media Manager)**:
   - Purpose: Handles external communications including emails, social posts, and release notes.
   - Registered in `openclaw.json` under ID `paddock`.
   - Workspace created at `/home/claw/.openclaw/workspace/agents/paddock`.

3. **Pitcrew (Git / Automation)**:
   - Purpose: Manages Git operations, builds Docker containers, and runs deployment pipelines.
   - Registered in `openclaw.json` under ID `pitcrew`.
   - Workspace created at `/home/claw/.openclaw/workspace/agents/pitcrew`.

### Gateway Update
- The Gateway must be restarted to apply changes and make the new agents visible in the OpenClaw UI.